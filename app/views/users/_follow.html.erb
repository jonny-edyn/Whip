<div class="row" style="margin-bottom: 20px; border: 1px solid rgb(192, 192, 192);border-left: none;">
	<div class="col-sm-2 bg-grey  text-uppercase" style="padding: 0px; border-right: 1px solid #c0c0c0;border-left:1px solid #c0c0c0;">
		<%= form_for(current_user.relationships.build(followed_id: @user.id)) do |f| %>
		  <div><%= f.hidden_field :followed_id %></div>
		  <%= f.submit "Follow #{@user.name}", class: "text-din-medium", style: "background: transparent none repeat scroll 0% 0%;width: 100%;height: 40px; border: none;" %>
		<% end %>
	</div>
	<div class="col-sm-10 text-din text-uppercase" style="padding: 10px;">
	</div>
</div>