<div class="container max_924 padding_bottom_60">

<%= render :partial => 'shared/top_banner_with_names', :locals => { :user => current_user } %>

	<h4 class="text-din padding_bottom_24">Users You Are Following</h4>
	<% 
		if @users.any?
		%>
		<ul class="list-group">
		<%
		@users.each do |user|
	%>
	<!--
	<div class="row padding_bottom_20">
		<div class="col-sm-2">
			<p style="margin: 6px 0px 10px;"><a href="<%= user_path(user.id) %>" style="font-size: 18px;"><%= user.name %></a></p>
		</div>
		<div class="col-sm-4">
			<div class="btn btn-default btn-square">
				<%= form_for(current_user.relationships.find_by(followed_id: user),
		             html: { method: :delete }) do |f| %>
				  <%= f.submit "Unfollow", class: "text-din-medium", style: "background: transparent none repeat scroll 0% 0%;width: 100%;height: 24px; border: none;" %>
				<% end %>
			</div>
		</div>
	</div>
	-->
	<li class="list-group-item">
		<div class="row">
			<div class="col-sm-2">
				<%# set the line-height to the height of the div to vertically center, in this case, the height of the button %>
				<a href="<%= user_path(user.id) %>" style="font-size: 18px;line-height:34px;"><%= user.name %></a>
			</div>
			<div class="col-sm-4">
				<%= form_for(current_user.relationships.find_by(followed_id: user),
					 html: { method: :delete }) do |f| %>
				  <%= f.submit "Unfollow", class: "btn btn-outline btn-square text-din-medium" %>
				<% end %>
			</div>
		</div>
	</li>

	<% end %>
	</ul>
	<% end %>
</div> <!--.container max_924 -->